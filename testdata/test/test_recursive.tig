let
function bsearch(left:int,right:int,c:int): int=
	if left=right then left
	else 
		let 
			var mid := (left + right) / 2
    	in 
			if left<c then bsearch(mid+1,right,c)
			else bsearch(left,mid,c)	
		end
in
    bsearch(1,5,3)
end